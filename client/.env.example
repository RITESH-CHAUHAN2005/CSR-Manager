# Copy to .env to configure the client.

# Use the live Express/MongoDB backend instead of the in-memory mock.
# Leave unset (or false) to run the client standalone with seed data.
VITE_USE_API=false
